<?php
$host = "localhost"; // اسم السيرفر
$user = "root"; // اسم المستخدم (افتراضيًا root)
$pass = ""; // كلمة المرور (اتركها فارغة إذا لم تقم بتعيين كلمة مرور)
$dbname = "ecommerce"; // اسم قاعدة البيانات

// إنشاء الاتصال
$conn = new mysqli($host, $user, $pass, $dbname);

// التحقق من الاتصال
if ($conn->connect_error) {
    die("فشل الاتصال بقاعدة البيانات: " . $conn->connect_error);
}

// تحديد الترميز UTF-8
$conn->set_charset("utf8");

// يمكنك إلغاء تعليق السطر التالي للاختبار إذا كان الاتصال ناجحًا
// echo "تم الاتصال بقاعدة البيانات بنجاح!";
?>
